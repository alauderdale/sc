%section
	.container
		.row
			.col-lg-12
				%h1.text-center
					Edit Liquor
		.row
			.col-sm-3
			.col-sm-6
				.panel
					.panel-body
						.padded
							= form_for(:liquor, :url => {:action => 'update_liquor', :id=> @liquor.id}, :html => { :multipart => true }) do |f| 
								= render(:partial => 'form', :locals => {:f => f})
								.form-buttons
									= submit_tag("Update", :class=>"btn btn-primary") 
			.col-sm-3