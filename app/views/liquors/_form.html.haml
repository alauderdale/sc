.form-group
	= f.label 'name'
	= f.text_field :name, :required => true, class: 'form-control'
.form-group
	= f.label 'description'
	= f.text_area :description, :rows => 5, class: 'form-control'
.form-group.margin-bottom
	%label
		Select Image
	= f.file_field :liquor_image