%section#liquor-header{:style=>"background:white url(#{@liquor.liquor_image.url(:medium)})center no-repeat; background-position-y:30px;"}
	.double-padding-top.double-padding-bottom
	.padding-top.padding-bottom
%section.no-padding-bottom
	.container
		.row
			.col-lg-12
				.text-center
					%a.btn.btn-primary.new-check-in-btn{"data-toggle"=>"modal", :href=>"#checkInModal", :rel=>"tooltip", "data-original-title"=>"Check-in with this scotch", "data-placement"=>"bottom"}
						+
					%h2.light-font-name.primary-text-color.no-margin-bottom
						=@liquor.name
					%p.text-muted
						%span.extra-bold-font-name
							=@liquor.age
						Year
					%p
						=@liquor.description
					%p.no-margin-bottom
						=link_to @liquor.distill,{:class=>"secondary-link"} do 
							by 
							=@liquor.distill.name
				-if current_user.admin
					.margin-top
						=render partial: 'show_actions', locals:{:liquor => @liquor}
%section.no-padding-top
	.container
		.row
			.col-lg-12
				%h5.page-header
					+ My Notes
		.row
			.col-sm-12
				-if @current_user_check_ins.count > 0
					-@current_user_check_ins.each do |c|
						=render partial: 'check_ins/post', locals:{:check_in => c}
				-else
					%p.text-muted.text-center
						No activity to show


/ %section
/ 	.container
/ 		.row
/ 			.col-sm-12
/ 				.row
/ 					.col-sm-9
/ 						.panel.margin-bottom
/ 							.panel-body
/ 								.margin-bottom
/ 									=render partial: 'liquor_thumb_medium', locals:{:liquor => @liquor}
/ 								%a.btn.btn-primary{"data-toggle"=>"modal", :href=>"#checkInModal", :rel=>"tooltip", "data-original-title"=>"Check-in this scotch", "data-placement"=>"bottom"}
/ 									%i.fa.fa-plus{:style=>"font-size:2em;"}


/ 					.col-sm-3
/ 						-if current_user.admin
/ 							.margin-bottom
/ 								=render partial: 'show_actions', locals:{:liquor => @liquor}
/ 				.row
/ 					.col-sm-9
/ 						.panel
/ 							.panel-body
/ 								.page-header.no-margin-top
/ 									%p.no-margin-bottom
/ 										%strong
/ 											My Recent Activity
/ 								-if @current_user_check_ins.count > 0
/ 									-@current_user_check_ins.each do |c|
/ 										=render partial: 'check_ins/post', locals:{:check_in => c}
/ 										%hr
/ 								-else
/ 									%p.text-muted
/ 										No activity to show
/ 					.col-sm-3

#checkInModal.modal{"aria-hidden" => "true", role: "dialog"}
	.modal-dialog
		.modal-content
			.modal-header
				%button.close{"aria-hidden" => "true", "data-dismiss" => "modal", type: "button"} ×
				%h4.no-margin-bottom.modal-title.light-font-name
					Check-In
			.modal-body
				=render partial: 'check_ins/check_in_form'
