%section
	.container
		-if current_user
			.row
				.col-lg-12
					.panel
						.panel-body
							.page-header.no-margin-top
								%h4.no-margin-bottom
									My Scotch List
							-@user_liquors.each do |l|
								=render partial: 'liquors/liquor_thumb_small', locals:{:liquor => l}

			/ .row
			/ 	.col-sm-9
			/ 		.panel
			/ 			.panel-body
			/ 				.page-header.no-margin-top
			/ 					%h4.no-margin-bottom
			/ 						My Recent Activity
			/ 				-if @check_ins.count == 0
			/ 					%p.text-center
			/ 						No Activity to show
			/ 				-@check_ins.each do |c|
			/ 					=render partial: 'check_ins/post', locals:{:check_in => c}
			/ 					%hr
			/ 	.col-sm-3
			/ 		.panel
			/ 			.panel-body
			/ 				.page-header.no-margin-top
			/ 					%h5.no-margin-bottom
			/ 						Recently Tasted
			/ 				-@recent_liquors.each do |c|
			/ 					=render partial: 'liquors/liquor_thumb_small', locals:{:liquor => c.liquor}
