%section
	.container
		.row
			.col-lg-12
				%h1.text-center
					Edit Note
		.row
			.col-sm-3
			.col-sm-6
				.panel
					.panel-body
						.padded
							= form_for(:check_in, :url => {:action => 'update_check_in', :id=> @check_in.id}, :html => { :multipart => true }) do |f| 
								= render(:partial => 'form', :locals => {:f => f})
								.form-buttons
									= submit_tag("Update", :class=>"btn btn-primary") 
			.col-sm-3